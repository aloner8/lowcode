# CONTEXT — ภาพรวมการเริ่มต้นใหม่

สถานะ: แผนภาพรวมรอบแรก • 12 กันยายน 2026

เอกสารนี้สรุปแนวคิดจาก Context Diagram ที่ผู้ใช้ให้มา เพื่อเริ่มทบทวนโครงการทีละส่วน หลังจากพัฒนาและทดสอบแล้วพบว่าหลายอย่างยังไม่ตอบโจทย์ เป้าหมายคือจัดบทบาทและเส้นทางการทำงานให้ชัด แล้วค่อยลงรายละเอียดแต่ละขา

เริ่มใหม่ในด้านการวางแผน โดยเก็บโค้ด เอกสาร และข้อมูลเดิมไว้ให้คัดเลือกใช้ต่อ ความสามารถเดิม รวมถึง [ShareModule.MD](ShareModule.MD) ยังอยู่ในขอบเขตโครงการ แต่ลำดับพัฒนาและวิธีประกอบระบบจะทบทวนตามภาพรวมนี้ เอกสารนี้อธิบายระบบที่ต้องการ ไม่ใช่การรับรองว่าโค้ดปัจจุบันทำได้ครบแล้ว

## 1. แนวคิดหลัก

**Dev Server ใช้ออกแบบและจัดการแอป โดยเก็บต้นแบบใน DB_main แล้วสร้าง App ลูกที่มีการตั้งค่าและฐานข้อมูลใช้งานของตัวเอง**

- Dev Server รู้ว่า User คนใดสร้าง App ใด ใช้แม่แบบอะไร และมีการออกแบบ/ตั้งค่าอย่างไร
- DB_main เก็บข้อมูลส่วนกลาง รวมถึงแบบหน้าจอและข้อมูลที่ใช้สร้าง App ลูก
- App ลูกแต่ละตัวมี config, path, port, domain และชื่อ DB ของตัวเอง
- ใช้ Database Server เดียวกันได้ แต่สร้างฐานข้อมูลแยกสำหรับแต่ละ App
- App ลูกเรียก `api/form` เพื่ออ่านแบบจาก DB แม่ แล้วแสดงเป็นหน้าเว็บแบบ Dynamic HTML
- App ลูกเรียก `api/db` เพื่อดำเนินการกับข้อมูลใช้งานเฉพาะของตัวเอง

ชื่อ `api/form` และ `api/db` ในรอบนี้ใช้สื่อบทบาทตามภาพ ยังไม่กำหนด URL, request/response หรือรูปแบบติดตั้งจริง

## 2. แผนภาพภาพรวม

```text
ผู้สร้างแอป
    |
    v
Dev Server / App แม่
    | บันทึกผู้สร้าง, แม่แบบ, แบบหน้าจอ, การทำงาน, การตั้งค่า
    v
DB_main ----------------------------------------------+
    |                                                 |
    | ข้อมูลสำหรับ Clone / สร้าง App ลูก              | อ่านแบบผ่าน api/form
    v                                                 |
App 1                 App 2                 App 3      |
    |                     |                     |      |
    +---------------------+---------------------+------+
    |                     |                     |
    | api/db              | api/db              | api/db
    v                     v                     v
DB_app_1              DB_app_2              DB_app_3

DB_main และ DB_app_1..3 อยู่บน Database Server เดียวกันได้
แต่เป็นคนละฐานข้อมูล และข้อมูลใช้งานแยกตาม App
```

เส้นทางอ่านแบบและข้อมูลต้องผ่าน API ฝั่ง server ของระบบ ส่วนการวาง API ไว้ที่ Dev Server หรือ Runtime ของ App ลูกจะลงรายละเอียดภายหลัง Browser ไม่เชื่อมฐานข้อมูลโดยตรง

## 3. บทบาทและขอบเขตข้อมูล

| ส่วน | หน้าที่หลัก | ข้อมูลที่รับผิดชอบ |
|---|---|---|
| Dev Server / App แม่ | ให้ผู้สร้างเลือกแม่แบบ ออกแบบหน้า กำหนดการทำงาน และจัดการ App ลูก | การแก้ไขแบบและการตั้งค่าผ่านเครื่องมือพัฒนา |
| DB_main | เป็นแหล่งข้อมูลกลางของการสร้างแอป | User ผู้สร้าง, ความสัมพันธ์ User–App, แม่แบบ, แบบหน้าจอ/การทำงาน และทะเบียน App |
| App ลูก | แสดงหน้าจอและทำงานตามแบบที่กำหนดสำหรับแอปนั้น | config ของแอป, ตัวตน App, path, port, domain และการอ้างอิงแบบ/DB |
| DB ของ App ลูก | เก็บข้อมูลที่เกิดจากการใช้งานจริงของแต่ละแอป | เช่น รายการคำร้อง เอกสาร และข้อมูลธุรกิจ |
| Shared Module | ให้แม่และลูกใช้ความสามารถพื้นฐานร่วมกัน | โค้ดโมดูลและสัญญาการใช้งาน โดยใช้ context และสิทธิ์ของแอปที่เรียก |

เว็บแม่มีผู้ใช้งาน 2 กลุ่มตาม [2.1 User.MD](User.MD): **Admin Server** จัดการบัญชี Developer และ Monitor Server, Docker, Nginx; **Developer (Customer)** สร้างแม่แบบ รัน App ตามโควต้าที่ได้รับ และตั้ง subdomain ของตนเอง

User ในระบบแม่กับผู้ใช้งาน App ลูกเป็นคนละบทบาท รายละเอียดบัญชีและการเชื่อมโยงสิทธิ์จะคุยในขา Auth ส่วนการเก็บ config ของ App ลูกจริงไว้ที่ใดจะตัดสินในขา Clone/Runtime

การใช้โค้ดหรือโมดูลร่วมกันไม่ได้ทำให้ข้อมูลของ App ต่าง ๆ ใช้ร่วมกันโดยอัตโนมัติ ฝั่ง server ต้องระบุ App และเลือก DB ตามสิทธิ์ของคำขอ

## 4. เส้นทางการทำงานหลัก

### ขา A — ออกแบบในระบบแม่

ผู้สร้างเข้า Dev Server → สร้างงานจากแม่แบบ → ออกแบบหน้าและการทำงาน → บันทึกใน DB_main โดยรู้เจ้าของและ App ที่เกี่ยวข้อง

ผลลัพธ์ที่ต้องได้: แบบและการตั้งค่าที่นำไปสร้าง App ลูกได้

### ขา B — Clone / สร้าง App ลูก

เลือกแบบต้นทาง → กำหนดตัวตน App, path, port, domain และชื่อ DB → เตรียมพื้นที่และ DB ของแอป → เปิด Runtime ของ App ลูก

ผลลัพธ์ที่ต้องได้: App ลูกหนึ่งตัวที่เชื่อมแบบถูกชุดและใช้ DB ของตัวเอง

คำว่า Clone ยังต้องกำหนดให้ชัดว่าคัดลอกอะไร เช่น โครงสร้าง DB, config หรือข้อมูลตั้งต้น และแบบหน้าจอเป็นสำเนาเฉพาะ App หรืออ้างอิงต้นแบบร่วมกัน รอบนี้ยังไม่ถือว่า Clone คือคัดลอก DB_main ทั้งก้อน

### ขา C — แสดงหน้าจอ

ผู้ใช้เปิด App ลูก → App เรียก `api/form` → ระบบอ่านแบบของ App นั้นจาก DB_main → Runtime แสดงหน้าแบบ Dynamic HTML พร้อมองค์ประกอบและการทำงานที่ออกแบบไว้

ผลลัพธ์ที่ต้องได้: เปิด App แล้วเห็นหน้าที่ตรงกับแบบและ config ของตัวเอง

ยังต้องกำหนดว่าการอ่านใช้แบบล่าสุดทันทีหรือแบบที่เผยแพร่แล้ว รวมถึง cache และพฤติกรรมเมื่อ DB_main ติดต่อไม่ได้

### ขา D — ใช้งานข้อมูล

ผู้ใช้ทำรายการบนหน้า → App เรียก `api/db` → server ตรวจสิทธิ์และเลือก DB ของ App → อ่าน/บันทึกข้อมูล → ส่งผลกลับหน้าจอ

ผลลัพธ์ที่ต้องได้: App 1 ใช้ข้อมูลใน DB_app_1 และไม่อ่านหรือแก้ข้อมูลของ App 2 โดยไม่ได้รับอนุญาต

### ขา E — ใช้ Shared Module

ผู้สร้างเลือกโมดูลและตั้งค่าที่จำเป็น → หน้า/การทำงานเรียกโมดูล → โมดูลทำงานภายใต้ App และผู้ใช้ปัจจุบัน → ส่งผลลัพธ์กลับ

คงเป้าหมายจาก ShareModule เดิม: ใช้ง่าย ตั้งค่าน้อย และใช้ implementation ร่วมกัน งาน Auth/Permission, Files, Mail, Google API, ภาพ/QR และการจัดการ version ยังอยู่ในขอบเขต โดยค่อยกำหนดรายละเอียดทีละกลุ่มหลังขาหลักชัดเจน

## 5. ของเดิมที่เก็บไว้พิจารณาใช้ต่อ

รายการนี้เป็นจุดเริ่มต้นจากไฟล์ที่มีอยู่ ยังต้องตรวจความเหมาะสมกับแบบใหม่เมื่อถึงขานั้น

| ส่วนเดิม | จุดอ้างอิง | แนวทาง |
|---|---|---|
| ตัวแสดงหน้าและ Component Registry | `src/components/engine/DynamicPageRenderer.tsx`, `src/lib/engine/ComponentRegistry.ts` | พิจารณาใช้เป็นฐานของการแสดงหน้า Dynamic |
| ตัวประมวลผลการทำงาน | `src/lib/engine/WorkflowInterpreter.ts` | ทบทวนเมื่อกำหนดการทำงานของหน้า |
| ระบบเชื่อม DB แยก | `src/lib/db/tenantDb.ts` | ตรวจระดับการแยกข้อมูลให้ตรงกับหนึ่ง App ต่อหนึ่ง DB |
| ทะเบียน App และการเปิด Runtime | `src/lib/runtime/siteRegistry.ts`, `scripts/run-sites.mjs` | ทบทวนการผูก App กับ port, domain, path และ DB |
| Shared Module | `packages/sharemodule/`, `ShareModule.MD` | เก็บความสามารถและเป้าหมายเดิม แล้วปรับการเชื่อมเข้ากับภาพรวมใหม่ |
| เอกสารและชุดทดสอบเดิม | `docs/`, `tests/` | ใช้เป็นประวัติและหลักฐานประกอบ ไม่ถือว่าข้อที่เคยผ่านเท่ากับผ่านแบบใหม่ |

จุดที่ต้องตรวจเป็นพิเศษ: โค้ด DB เดิมมีทั้ง runtime snapshot และ design config ในฐานข้อมูลฝั่ง tenant และบางส่วนอ้างอิงระดับ platform ขณะที่ภาพใหม่นี้ระบุให้ App อ่านแบบจาก DB_main และมี DB ของตัวเอง จึงต้องตกลงความสัมพันธ์ระหว่างแม่แบบกับ App รวมถึงแหล่งแบบที่ Runtime ใช้ ก่อนนำกลไกเดิมกลับมาใช้

## 6. ลำดับการคุยและลงรายละเอียด

| Step | เรื่องที่จะทำ | ผลลัพธ์ที่ต้องได้ก่อนเดินต่อ |
|---|---|---|
| 1 — รอบนี้ | Context และขอบเขตภาพรวม | เข้าใจบทบาทแม่, DB_main, App ลูก, DB ลูก และ Shared Module ตรงกัน |
| 2 | User → แม่แบบ → App | เริ่มที่ [2.1 บทบาท User](User.MD) แล้วต่อด้วยความเป็นเจ้าของและการสร้างหลาย App จากแม่แบบ |
| 3 | Clone และการเตรียม App | รายการสิ่งที่คัดลอก/อ้างอิง พร้อมตำแหน่ง config, path, port, domain และ DB |
| 4 | แบบหน้าจอ → api/form → Runtime | วิธีอ่านแบบ การเผยแพร่การแก้ไข และหน้าตัวอย่างหนึ่งหน้า |
| 5 | หน้าจอ → api/db → DB ลูก | การอ่าน/บันทึกหนึ่งรายการ และขอบเขตสิทธิ์ระหว่าง App |
| 6 | Shared Module ทีละกลุ่ม | เลือกงานแรก กำหนด input/output แล้วทดลองกับแม่และลูก |

รายละเอียด schema, API contract, โครงสร้าง package และแผนย้ายระบบจะกำหนดเมื่อถึงส่วนที่เกี่ยวข้อง

หลักฐานชุดแรกเมื่อเริ่มพัฒนา: ใช้แม่แบบเดียวสร้าง App 1 และ App 2 เปิดหน้าตามแบบได้ และบันทึกข้อมูลแล้วแยก DB กันจริง จากนั้นจึงขยายความสามารถทีละขา
