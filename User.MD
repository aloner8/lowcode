# 2.1 User — ผู้ใช้งานเว็บแม่

สถานะ: ข้อกำหนดบทบาทและหน้าจอหลัก Admin Server และ Developer (Customer) • 12 กันยายน 2026

อ้างอิงภาพรวม [CONTEXT.MD](CONTEXT.MD) เว็บแม่มีผู้ใช้งาน **2 กลุ่ม** คือ **Admin Server** และ **Developer (Customer)** โดย Developer กับ Customer หมายถึงกลุ่มเดียวกันในเอกสารนี้

## 1. Admin Server

ผู้ดูแลระบบส่วนกลาง มีหน้าที่:

- จัดการบัญชี Developer (Customer) รวมถึงเพิ่มและลบ User
- ดูรายละเอียด Customer และระงับการใช้งานบัญชี (Suspend)
- สวมสิทธิ์เป็น Customer เพื่อเข้าใช้งานภายใต้สิทธิ์ของ Customer ที่เลือก
- Monitor สถานะ Server
- Monitor สถานะ Docker
- Monitor สถานะ Nginx

รายละเอียดสิทธิ์สั่งงาน Server, Docker และ Nginx เช่น restart หรือแก้ config จะกำหนดภายหลัง รอบนี้ยืนยันหน้าที่ติดตามสถานะก่อน

### 1.1 หน้าจอหลักหลัง Login

เมื่อ Admin Server Login สำเร็จ ให้เข้าสู่หน้าจอหลักที่ **ไม่มีเมนูด้านซ้าย** โดยเรียงเนื้อหาจากบนลงล่างดังนี้

**แถวแรก — แดชบอร์ดสรุปภาพรวม**

| รายการ | ข้อมูลที่แสดง |
|---|---|
| สถานะ Server | สถานะปัจจุบันของ Server |
| จำนวน Customer | จำนวน Customer ทั้งหมดในระบบ |
| จำนวน App | จำนวน App ทั้งหมดในระบบ |

**ส่วนถัดมา — ตารางรายชื่อ Customer**

หนึ่งแถวต่อหนึ่ง Customer โดยแสดงข้อมูลและปุ่มดำเนินการดังนี้

| คอลัมน์ | ข้อมูลหรือการทำงาน |
|---|---|
| ชื่อ | ชื่อ Customer |
| โดเมนหลัก | โดเมนหลักของ Customer |
| จำนวนแม่แบบ | จำนวนแม่แบบที่ Customer เป็นเจ้าของ |
| จำนวน App | จำนวน App ที่ Customer เป็นเจ้าของ |
| Detail | เปิดดูรายละเอียดของ Customer พร้อมรายการแม่แบบและ App ของ Customer นั้น |
| สวมสิทธิ์เป็น Customer | เข้าใช้งานในฐานะ Customer ที่เลือก โดยใช้สิทธิ์และขอบเขตข้อมูลของ Customer นั้น |
| Suspend | ระงับการใช้งานบัญชี Customer ที่เลือก |

รายละเอียดหน้าจอ Detail และผลของ Suspend ต่อ session ที่ใช้งานอยู่หรือ App ที่กำลังรัน จะกำหนดในขั้นถัดไป

## 2. Developer (Customer)

ผู้ใช้บริการเว็บแม่เพื่อสร้างและรันแอปของตนเอง มีหน้าที่และความสามารถ:

- สร้างแม่แบบของตนเอง
- สร้าง App จากแม่แบบตามภาพรวมโครงการ และรัน App ตามโควต้าที่ได้รับ
- สั่ง Start / Stop App ของตนเอง
- ดูจำนวนแม่แบบ, App, Database และการใช้ทรัพยากรของตนเอง
- ตั้ง subdomain ของตนเองสำหรับเข้าใช้งาน App

แม่แบบและ App ต้องระบุ Developer เจ้าของ เพื่อใช้แสดงรายการของตนเอง ตรวจสิทธิ์ และนับการใช้โควต้า

### 2.1 หน้าจอหลักหลัง Login

เมื่อ Developer (Customer) Login สำเร็จ ให้เข้าสู่หน้าจอหลักที่ **ไม่มีเมนูด้านซ้าย** โดยข้อมูลทั้งหมดอยู่ในขอบเขตของ Customer ที่กำลังใช้งาน รวมถึงกรณี Admin Server สวมสิทธิ์เป็น Customer นั้น

**แถวแรก — แดชบอร์ดสรุปภาพรวม**

| รายการ | ข้อมูลที่แสดง |
|---|---|
| จำนวนแม่แบบทั้งหมด | จำนวนแม่แบบทั้งหมดที่ Customer เป็นเจ้าของ |
| จำนวน App ทั้งหมด | จำนวน App ทั้งหมดที่ Customer เป็นเจ้าของ |
| จำนวน Database | จำนวนฐานข้อมูลของ App ที่ Customer เป็นเจ้าของ |
| การใช้ทรัพยากร | สรุปการใช้ทรัพยากรของ Customer |

ชนิดทรัพยากร หน่วยวัด และช่วงเวลาที่ใช้สรุป จะกำหนดในขั้นถัดไป

**ส่วนถัดมา — ตาราง APPS จัดกลุ่มตามแม่แบบ**

ตารางหลักแสดงหนึ่งแถวต่อหนึ่งแม่แบบของ Customer โดยแต่ละแม่แบบมีตารางย่อย (Sub table) แสดง App ที่สร้างจากแม่แบบนั้น

| คอลัมน์ตารางหลัก | ข้อมูลหรือการทำงาน |
|---|---|
| ชื่อแม่แบบ | ชื่อแม่แบบที่ Customer เป็นเจ้าของ |
| Detail | เข้าไปจัดการแม่แบบที่เลือกตามหัวข้อ 2.2 |

| คอลัมน์ตารางย่อย | ข้อมูลหรือการทำงาน |
|---|---|
| ชื่อ App | ชื่อ App ที่สร้างจากแม่แบบของแถวหลัก |
| Detail | เข้าไปจัดการ App ที่เลือก |
| Start | เริ่มรัน App ที่เลือก ภายใต้สิทธิ์และโควต้าของ Customer |
| Stop | หยุดรัน App ที่เลือก |

ปุ่ม Start / Stop เป็นคำสั่งระดับ App ส่วนรายละเอียดหน้าจอจัดการ App จะกำหนดในขั้นถัดไป

### 2.2 หน้าจัดการแม่แบบที่เลือก

เข้าจากปุ่ม Detail ของแม่แบบในตาราง APPS หน้านี้เป็นหัวใจสำคัญของระบบ ใช้ออกแบบและจัดการทุกส่วนภายในแม่แบบที่เลือก โดย **มีเมนูด้านบนและเมนูด้านซ้าย** ข้อกำหนดไม่มีเมนูซ้ายในหัวข้อ 2.1 ใช้กับหน้าจอหลักหลัง Login

**เมนูด้านบน**

| ส่วน | การทำงาน |
|---|---|
| ค้นหา Object | ค้นหา Object ต่าง ๆ ภายในแม่แบบที่กำลังจัดการ |
| สถานะ Public | แสดงว่าแม่แบบเปิดเป็น Public อยู่หรือไม่ และแก้ไขสถานะนี้ได้ |

ขอบเขตการมองเห็นและการนำแม่แบบ Public ไปใช้จะกำหนดเพิ่มเติมภายหลัง

**เมนูด้านซ้าย — เรียงตามลำดับ**

| ลำดับ | เมนู | การทำงาน |
|---|---|---|
| 1 | On Start Up | กำหนดค่าตอนเริ่ม Load App ได้แก่ ค่าเริ่มต้น, ConnectionString และ Main CSS UI |
| 2 | Module Setting | เลือกใช้โมดูลมาตรฐานตาม [ShareModule.MD](ShareModule.MD) พร้อมกำหนดค่าและขอบเขตการใช้งานตามต้องการ |
| 3 | Site Map View | แสดงโครงสร้าง App แบบ Tree view ตามลำดับ Route → Screen → Page → AppComponent |
| 4 | Collection Set | จัดการเกี่ยวกับ Database และสร้าง Form อัตโนมัติจาก Collection ได้ |
| 5 | Screens | เพิ่ม ลบ แก้ไข Screen ของ App และกำหนดโครงหน้าจอ รายละเอียดต่อใน [Screen.MD](Screen.MD) |
| 6 | Pages | เพิ่ม ลบ แก้ไข Page ของ App โดยนำ Component มาประกอบ และ Load Collection ได้มากกว่าหนึ่งชุด รายละเอียดต่อใน [Pages.MD](Pages.MD) |
| 7 | Components | เพิ่ม ลบ แก้ไข Component ของ App โดยใช้ HTML และนำ Component Standard มาประกอบได้ รายละเอียดต่อใน [Components.MD](Components.MD) |

**ระดับของ Site Map View**

| Level | สิ่งที่แสดง |
|---|---|
| 1 | Route |
| 2 | Screen ที่ Route นั้นใช้ |
| 3 | Page ต่าง ๆ ภายใน Screen นั้น |
| 4 | AppComponent ต่าง ๆ ภายใน Page นั้น |

**ขอบเขตการตั้งค่า Screens เบื้องต้น**

- Top: กำหนดการแสดงผลส่วนบน
- Left side: กำหนดการแสดงผลด้านซ้าย
- Right side: กำหนดการแสดงผลด้านขวา
- Content: กำหนดว่าจะใช้ Page ใดแสดงเนื้อหา
- Footer: กำหนดการแสดงผลส่วนท้าย
- Popup: กำหนด Popup ที่เตรียมไว้สำหรับเปิดใช้งาน
- CSS หลัก: กำหนด CSS หลักของ Screen

รายละเอียดเชิงลึกของ Screen, Page และ Component จะทยอยกำหนดในเอกสารแยก ส่วนการนำการแก้ไขแม่แบบไปใช้กับ App ที่สร้างไว้แล้วจะกำหนดในขา Clone/Runtime ตาม [CONTEXT.MD](CONTEXT.MD)

## 3. ขอบเขตการใช้งาน

| เรื่อง | Admin Server | Developer (Customer) |
|---|---|---|
| เพิ่ม/ลบบัญชี Developer | จัดการได้ | ไม่มีหน้าที่จัดการบัญชี Developer อื่น |
| ดูรายละเอียด Customer | ดูรายละเอียดและรายการแม่แบบ/App ของ Customer ได้ | เข้าถึงข้อมูลของตนเอง |
| ระงับบัญชี Customer (Suspend) | ระงับบัญชี Customer ได้ | ไม่มีสิทธิ์ระงับบัญชี Customer อื่น |
| สวมสิทธิ์เป็น Customer | เข้าใช้งานภายใต้สิทธิ์ของ Customer ที่เลือกได้ | ไม่มีสิทธิ์สวมสิทธิ์เป็น Customer อื่น |
| Monitor Server, Docker, Nginx | ดูสถานะระบบส่วนกลาง | ดูการใช้ทรัพยากรของตนเองตามแดชบอร์ด โดยรายละเอียด Monitor เฉพาะ App จะกำหนดภายหลัง |
| สร้างแม่แบบ | ทำได้ตามสิทธิ์ของ Customer เมื่อสวมสิทธิ์ | สร้างแม่แบบของตนเอง |
| รัน App | ทำได้ตามสิทธิ์และโควต้าของ Customer เมื่อสวมสิทธิ์ | รัน App ของตนเองตามโควต้าที่ได้รับ |
| Start / Stop App | ทำได้ตามสิทธิ์ของ Customer เมื่อสวมสิทธิ์ โดย Start ต้องอยู่ภายในโควต้า | เริ่มหรือหยุดรัน App ของตนเอง โดย Start ต้องอยู่ภายในโควต้า |
| ตั้ง subdomain | รายละเอียดการดูแลส่วนกลางกำหนดภายหลัง | ตั้ง subdomain ของตนเอง |

ผู้ใช้งานปลายทางของ App ลูกจะกำหนดแยกในขา Auth ของ App ลูก ไม่เพิ่มเป็นกลุ่มที่สามของเว็บแม่

## 4. โควต้าและ subdomain

**ข้อกำหนดที่ยืนยันแล้ว:** Developer รัน App ได้ตามโควต้าที่ได้รับ และตั้ง subdomain ของตนเองได้

รายละเอียดที่ต้องกำหนดในขั้นถัดไป:

- ใครเป็นผู้กำหนดและปรับโควต้าให้ Developer
- โควต้านับจำนวน App ที่สร้างทั้งหมด หรือจำนวน App ที่กำลังรัน และมีข้อจำกัดทรัพยากรอื่นด้วยหรือไม่
- เมื่อโควต้าเต็มหรือลดลง ระบบจัดการ App ที่มีอยู่อย่างไร
- subdomain อยู่ใต้โดเมนหลักใด และหนึ่ง Developer/หนึ่ง App มีได้กี่ชื่อ
- กติกาการจองชื่อซ้ำ เปลี่ยนชื่อ และคืนชื่อเมื่อเลิกใช้
- เมื่อลบ Developer จะจัดการแม่แบบ, App, DB และ subdomain ที่เป็นเจ้าของอย่างไร การลบบัญชีจึงยังไม่ถือเป็นคำสั่งลบทรัพย์สินทั้งหมดอัตโนมัติ

เอกสารนี้กำหนดบทบาท ขอบเขต หน้าจอหลักของ Admin Server และ Developer (Customer) รวมถึงหน้าจัดการแม่แบบ ยังไม่กำหนด schema, API หรือเปลี่ยนระบบสิทธิ์ในโค้ดเดิม
